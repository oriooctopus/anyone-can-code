import { ThemeType } from '../themes.types';
import { text } from './text';
import { sublessonInstructions } from 'src/styles/themes/defaultDark/sublessonInstructions';

export const themeDefaultDark: ThemeType = {
  primaryColor: 'red',
  text,
  sublessonInstructions,
};
