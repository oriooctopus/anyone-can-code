name: Lint & prettier!

on:
  push:
  pull_request:

jobs:
  deploy:
    name: Run linters
    runs-on: ubuntu-latest

    steps:
      name: 'Deploy to Netlify'
      uses: jsmrcaga/action-netlify-deploy@v1.1.0
